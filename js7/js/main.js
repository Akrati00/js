// conditionals: If Statements

// syntax

/* if(condition){
    run some code
} else {
    run some code
} */

// Example

// let soup = "corn soup";
let soup;
let reply;
if (soup){
     reply = `Here's your order of ${soup}`;
} else{
    reply = "Sorry, we're out of soup.";
}

console.log(reply);
 
